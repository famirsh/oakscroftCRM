"use client";

/**
 * Dashboard subscription tile — thin re-export of the reusable PlanWidget
 * so existing imports keep working.
 */
export { PlanWidget as SubscriptionCard } from "@/components/subscription/plan-widget";
